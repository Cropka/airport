#include "animation.h"

Animation::Animation(QWidget *parent) : QMainWindow(parent)
{
   // animate->plane->setPos(100,100);

    //plane2 = animate->scene->addPixmap(*plane_pic1);

}

Animation::~Animation()
{


}
